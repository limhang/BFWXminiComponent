<view class="container">
  <block wx:for="{{dataSource}}" wx:key="{{index}}">
    <view class="container-item cellSeparatorForward" bindtap="selectedItem" data-info="{{index}}">{{dataSource[index]}}</view>
  </block>
</view>
